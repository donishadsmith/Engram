[package]
name = "gameboy-emulator"
version = "0.1.0"
edition = "2024"

[dependencies]
hound = "3.0"
macroquad = { version = "0.4", features = ["audio"] }
spin_sleep = "1.0"
rfd = "0.17"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"